{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o bin/server main.go",
    "lint": "go vet ./... && gofmt -l . && staticcheck ./...",
    "test": "go test -v -race ./...",
    "clean": "rm -rf bin"
  }
}
